service: aws-node-express-api

plugins:
  - serverless-esbuild

provider:
  name: aws
  runtime: nodejs20.x

functions:
  express:
    handler: src/handler.handler
    url: true
    events:
      - httpApi: "*"
